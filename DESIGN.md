# SustLink · Desktop design

## 两种阅读节奏

校园入口以大块留白、平面色块和自然纵向滚动构成四章校园漫游。论坛以紧凑的平面主题列表、三列分工和清晰的文字层级承载讨论。校园页面使用苔绿与纸白，论坛使用蓝白。此方向来自本次用户已确认的方案，不改变现有代买小程序的暖色设计。

## 字号

| 语义                     | 大小与字重                |
| ------------------------ | ------------------------- |
| 入口主标题               | 46px / 600                |
| 页面、地标与帖子详情标题 | 24px / 600                |
| 帖子列表标题             | 15px / 500                |
| 正文与楼层回复           | 16px / 400，行高 1.75     |
| 导航、控件               | 14px，窄桌面部分导航 13px |
| 时间、浏览数、楼层号     | 13px                      |

英文品牌用本地打包的 Montserrat 400/500/600；中文回退至苹方、微软雅黑。大章节数字用 Georgia。更小的英文眉题、装饰编号只提供次要视觉提示。

## 颜色与组件

- 校园纸白 `#F7F8F2`，主墨色 `#254B52`，植被灰绿、湖水浅蓝，少量杏色点缀。
- 论坛背景 `#F5F7F9`，主色 `#356D9C`，选中浅底 `#EDF4F9`，分隔线 `#E8EDF0`。
- 主题列表使用连续平面行、细分隔线；取消独立卡片阴影、间隙和悬停位移。首页与板块页只显示标题及必要信息，不显示正文摘要、图片或视频预览，常规行高约 80px。搜索与个人中心保留两行摘要用于查找内容。回复数与浏览量并入底部信息栏。点击仅有约 160ms 的浅色底色变化，键盘导航保留内嵌焦点边框。详情正文保持分段。按钮约 5–7px，插画入口按钮 3px。论坛欢迎横幅高 136px，减少首屏装饰占用。
- 七个一级分类按用户参考图排序，导航最小高度 46px。论坛整体最大宽度 1200px；1440px 下中栏约 752px，侧栏宽 180px / 220px；1024px 侧栏宽 174px / 194px，正文列自适应，隐藏非必要的最近动态列。
- 点赞使用粉色实心爱心、收藏使用黄色实心书签和约 260ms 的淡入缩放。搜索框通过容器 focus-within 显示统一边框与光环，内部输入不重复描边。减少动态效果时取消位移动画。
- 上传入口分为图片、视频、附件，提供类型、文件名、大小与删除操作；图片原生对话框放大，视频不自动播放，文件可下载。头像支持点击与键盘操作。
- 发布页的标题输入框、正文编辑框、板块选择框和上传控件取消粗轮廓及光晕；悬停／聚焦统一为浅底色，按下时略加深，不缩放或抬升。键盘聚焦仍通过底色保持可见，高对比度模式使用系统 Highlight 背景色。标题最多 20 个可见字符，输入法组合过程不提前截断。
- 回复工具栏包含排序下拉框与只看楼主；每个回复楼层有独立点赞按钮，继续使用粉色心形。排序后保留原楼层号，点赞同分时按发布时间正序。
- 列表统计区由两列 66px 的统计单元和 10px 间距组成，标签固定、数字右对齐，采用等宽数字。数量从 1 万起保留一位小数并省略尾部 `.0`，悬停提示完整数量。
- 主题标题末尾使用 14px 附件类型图标（图片、视频、文件），每类一个，悬停显示数量；不显示正文、文件名或媒体预览。图标与标题共同作为进入主题的入口。
- 主题详情页取消控件聚焦时的粗轮廓和光晕，用浅色背景表达悬停、按下和键盘焦点；不产生抬升或布局位移。发布页也采用统一的背景反馈。
- 个人回复记录、引用块、公告和今日话题支持整容器点击，文件附件支持整行下载；保留独立次级链接与原生视频控制。发布页返回入口按实际来源命名。
- 所有交互具备可见焦点；登录使用原生 dialog 管理焦点与 Escape 关闭。正文纯文本显示。

## 滚动与 SVG

二级分类采用按需展开的行内筛选区，由主题排序栏右侧「分类筛选」按钮控制；按钮始终显示小图标＋「分类」。展开区采用浅底色、13px 文字与可换行按钮，不用悬停菜单。首页提供板块与分类两行，专属板块页仅一组分类。列表隐藏二级分类；全部讨论、搜索与个人主页仅保留一级板块，专属板块页省略重复板块标识。收起筛选区后，有筛选条件才显示一条可清除的摘要；右侧统计仍保持固定列宽。发布页的板块和分类选择放在同一行，窄桌面草稿状态独立换行。

个人主页入口采用用户轮廓图标，替换原侧栏书签入口；收藏继续位于个人主页内部。主帖与回复的删除按钮统一为 15px 垃圾桶图标＋「删除」，沿用当前操作栏高度与圆角，并通过 `margin-left: auto` 靠右；不改变点赞与引用的位置。详情从个人主页打开时，来源返回入口显示「个人主页」。

所有章节正常滚动，无滚轮接管、强制吸附或自动跳转。每个 SVG 带有命名分层和 `data-depth`；只修改透明度与 transform，离屏取消帧更新，卸载清理滚动与媒体查询监听。内嵌 SVG 的 ID 自动加实例前缀，避免多场景的图层／纹理 ID 冲突。系统减少动态效果时图层完整静态显示。

独立 SVG 文件本身可直接预览，不依赖 JS。页面文案不烘焙进插画，所有地标名称都保留为可选择文字。

列表底部不显示「共 X 条」总数。只有一页（含空列表）时不渲染分页栏；多页保留右对齐的页码、上一页和下一页。适用于论坛、板块、搜索及个人主页的笔记／回复／收藏列表。顶部筛选数量、搜索结果数量和个人资料统计保持现状。

## 容器内文字对齐（2026-09-08）

论坛列表单行笔记约 81px，文字块上下各 14px，分隔线 1px；头像与标题／元信息整体居中。列表作者与右侧回复／浏览统计分列，不显示发布时间及其分隔点；昵称按实际渲染宽度省略，仅超过可用宽度（上限 8em）时显示省略号，详情保留完整昵称。个人主页笔记与收藏使用相同组件，有摘要／附件时头像与标题对齐、内容自然增高。

主内容面板统一 24px 左右内边距，列表区域 20px；窄桌面分别为 20px 和 16px。个人资料、标签栏、列表、回复记录，搜索标题与表单，详情楼层与编辑器共享各自面板边线。控件文字使用 20px 行高，删除与点赞按钮保持相同边框占位，正文继续 1.75 行高。

## 搜索与详情去重（2026-09-08）

搜索页与顶部搜索框共用容器悬停、聚焦微光，内部输入不单独描边；个人资料输入取消粗轮廓并保留浅底色焦点。搜索页只保留一个分类按钮，展开后显示板块／分类两组，收起后显示完整路径摘要。清除筛选保留关键词，切换板块清除旧分类并回到第一页。

详情页仅显示不可点击的一级板块提示，隐藏二级分类。笔记发布时间只保留在楼主姓名下方；每条回复的发布时间属于独立信息，继续保留。

## 内容展示与编辑静态背景（2026-09-08 最新调整）

所有笔记列表与个人回复记录不显示发布时间。昵称保留完整文本，通过 CSS text-overflow 在实际宽度超过容器时省略；不按中英文字数截断，不修改存储或详情身份显示。详情分类区仅保留一级板块 span；hover 样式只作用于真实可点击的 a.board-tag。

标题、正文、回复与资料编辑文字字段背景固定 #FDFEFE，hover／active／focus 不改变底色、不出现粗轮廓。键盘焦点只改变现有 1px 边框颜色；回复与私聊输入框采用下述 2026-09-19 微光规则。高对比度模式使用系统颜色。搜索框、上传按钮、排序、点赞等交互控件保持原反馈。

## 分栏滚动与私聊输入（2026-09-19）

桌面端（宽度大于 760px）论坛顶栏固定，中间阅读区、左侧导航和右侧信息栏各自使用浏览器原生滚动，触达边界不将滚动传递给其他栏。内容少时不强行显示滚动条；不拦截或模拟滚轮事件。窄屏保持页面自然滚动。入口校园漫游不采用此分栏规则。

帖子列表标题统一 15px，覆盖板块、搜索与个人主页列表；详情标题保持原有层级。回复与私聊输入框聚焦时维持原尺寸和底色，使用 #A9BECA 边框与 2px、10% 透明度的浅蓝微光，取消粗轮廓。高对比度模式保留系统焦点提示。

桌面私聊页在中栏可用高度内布置卖家信息、可点击商品摘要、独立滚动的聊天记录及输入区。常规窗口高度下输入区保持可见；极矮窗口允许中栏滚动以保证所有操作可达。聊天记录支持滚轮与键盘翻阅，返回后恢复记录位置。列表返回、筛选与切换发布板块均记录实际阅读容器位置。

跳蚤市场每次进入时默认收起分类栏，点击「分类」展开分类与价格排序。私聊顶部入口显示「返回」，与商品摘要共用返回商品详情的行为；商品已删除时退回消息列表。姓名与时间、姓名与楼层之间使用空白间距，不再添加中点分隔。

商品返回链路固定为「聊天 → 商品详情 → 跳蚤市场列表」，这两个返回入口均显示「返回」。来源列表的筛选条件及阅读位置随聊天链路传递，详情不再提供「返回聊天」入口；再次聊天仍从「聊一聊」进入。

商品详情标题为 20px / 28px 行高。所有板块详情正文与楼层回复使用 16px / 24px（1.5 倍行高），段落间距 8px；段内单次换行保留，空行仅作为段落边界展示，原始存储内容不变。后续排版修改说明注明字号、行高及段间距。

## 分类发布表单（2026-09-14）

跳蚤市场、社团活动、失物招领的发布页使用已确认的独立发布方案，接入论坛 `/new`。仅集成发布表单及交互，论坛列表、详情和右栏内容展示保留原样。中栏沿用 `.content-panel` 与 `--panel-inset`：标题、分区标题、表单和底部按钮共享边线；控件继承论坛字体，14px / 22px，统一 46px 高度，窄屏自动改为单列。

三页的分类、时段、交接方式和板块选择复用 `publishing/controls.js` 的共享浮层，白底、灰蓝字体、浅蓝选中项与对勾。后续同类发布控件继续复用该组件，避免系统原生菜单造成字体和颜色突变。样式仅作用于发布页与专属浮层，不覆盖列表或详情控件。

日期固定六行、42 格；点击年月标题选择年份，点击任意年份（含原年份）返回月历，底部左侧清除、右侧今天。社团日期右侧保留平滑时分滚轮、鼠标拖拽与停止后对齐，禁选值、键盘操作和提交共用时间限制。只在整个表单底部保留一个问号，提示“请勿发布敏感信息”，悬浮 500ms 显示，点击或键盘聚焦立即显示。附件大小限制常显，标题及正文保持静态背景；有效必填项的星号隐藏。
