# 验收记录

初版日期：2026-09-06；最近验收：2026-09-08。环境：Windows、Node.js 24.14、Chrome 无头浏览器、Vite 本地开发服务。

2026-09-07 补充：按最新反馈恢复连续平面列表，移除帖子悬停位移与阴影；首页和板块页仅显示标题与必要元信息，不渲染正文或媒体预览。常规行高约 80px，点击仅有浅色底色反馈。修复底部回复数／浏览量被旧样式限制宽度导致的裁切。通过生产构建与桌面交互专项测试，并复查 1440px、1024px 截图：列表无间隙、悬停位置不变、正文预览不存在、统计文字完整、整行与板块标签分别正常跳转。

## 自动检查

2026-09-08 删除权限补充：11 项数据层测试、生产构建及 7 项相关浏览器测试通过（18.1 秒）。覆盖楼主删除自己笔记下他人的回复、禁止跨笔记删除他人回复、保留作者删除自身回复权限；确认笔记和回复删除后不出现成功 Toast，写入失败仍保留内容并给出反馈。此为删除权限阶段的专项记录。

- `npm test`：12 项数据层测试通过，新增二级分类归属、作者权限和旧内容兼容校验。覆盖发布与引用、游客与空输入拦截、跨主题引用拒绝、匿名身份稳定性、搜索筛选、置顶优先、存储往返、旧分类迁移、文件内容验证与上传大小限制，以及 20 字标题、可见字符计数、旧标题兼容、数字缩写、回复点赞和排序。
- `npm run build`：TypeScript 严格类型检查与 Vite 生产构建通过。主脚本 gzip 约 112 KB；字体、图标与插画随构建输出，无运行时外部资产依赖。
- `npm run typecheck`：独立类型检查通过。
- `npm run test:e2e`：本轮完整运行 30 组 Chrome 浏览器测试全部通过（1.9 分钟），覆盖二级分类与原有功能回归。
- 视频测试改为使用已验证可播放的固定本地样本 `tests/fixtures/campus.webm`；生成脚本为 `tests/make-video-fixture.mjs`。不再在每次回归中临时录制，避免测试样本受录制时序影响。
- 当前整体宽度上限 1200px、中栏约 752px；首页是约 80px 高的连续标题行，无摘要、间隙或阴影。已复查 1440px 和 1024px 截图。

## 浏览器场景

1. 四章校园入口、章节滚动、任意章节进入论坛、减少动态效果、1440px 与 1024px 桌面布局、20 条分页、深链接刷新、无效主题链接。
2. 演示登录、草稿刷新恢复、纯文本预览、发布、引用回复、点赞／收藏刷新保留、个人资料编辑、个人回复跳转楼层、搜索空结果、通知全部已读、空回复提示。
3. 匿名板块列表、楼层、引用与通知的昵称遮蔽，以及匿名回复身份稳定显示。
4. 模拟浏览器存储配额失败：页面可继续操作，警告可见，草稿标记为暂存本页。
5. Tab 键访问与焦点、快速正反滚动、浏览器前进后退、只看楼主过滤。
6. 损坏的本地存储：明确提示并回退演示数据，在用户操作前不覆盖原记录。
7. 图片、可播放 WebM、PDF/ZIP/Word 文件添加、草稿刷新、预览、图片放大与 Escape 关闭、仅文件发布、原生视频播放、下载文件内容一致、带图片回复、缩略图数量、文件缺失反馈。
8. 点击个人头像打开选图、居中裁切 256px、刷新恢复、顶部与普通帖子同步；匿名主题不显示关联头像。
9. 从分页列表、带筛选的搜索、个人中心和详情进入发布页，刷新后退出仍返回来源；保存草稿返回恢复滚动位置，浏览器前进保留草稿。
10. 七个分类顺序、整卡空白处点击跳转、板块标签独立跳转、旧分类地址跳转、卡片间距与阴影、搜索容器焦点、点赞收藏状态与减少动态效果。
11. 不支持的格式、空文件、文件移除、模拟 IndexedDB 写入失败：显示错误，未保存的文件不会出现在草稿，已有文字保留。
12. 发布页板块选择框、标题框、正文框获得焦点时无粗轮廓，保持 1px 基础边框，统一使用柔和背景反馈；上传按钮也没有轮廓和阴影，按下不缩放；20 字输入限制涵盖粘贴、中文输入法组合过程和组合表情。
13. 超过 20 字的旧草稿完整保留并显示缩短提示，暂不能发布；既有长标题帖子不被截断或重置。
14. 回复点赞的游客登录拦截、点赞／取消、本地刷新保留、三种排序与 URL 恢复、只看楼主、排序后引用楼层和新回复楼层编号保持正确。
15. 回复／浏览固定列在 1440px、1024px 下对齐，0、9999、1万、1.7万、1.8万、1亿格式正确，统计内容不裁切；首页继续不显示正文摘要。
16. 发布页根据来源显示板块名、论坛首页、搜索结果、收藏或主题标题，刷新后仍能正确返回原路径与查询条件。
17. 1440px／1024px 下标题末尾的图片、视频、文件图标位置正确，每种类型最多一个；悬停说明包含数量，点击可进入主题，列表不出现媒体或正文预览。
18. 详情排序、正文输入、上传、发布回复与点赞控件获得焦点时没有轮廓和阴影；键盘仍可访问上传入口并获得背景反馈，回复排序功能正常。
19. 点击个人回复记录、引用块、消息行、右侧话题及公告卡、搜索结果行的留白可进入对应内容；引用保持楼层定位，板块标签和公告列表入口仍独立跳转。文件附件容器留白触发下载，文件字节与上传内容一致。

## 视觉复查

二级分类验收：覆盖七个板块分类选项、仅点击展开、键盘操作、切换分类重置页码且保留排序、筛选总数、分页后进入详情及排序刷新再返回、旧内容未分类兼容、无效分类恢复、发布分类带入与草稿刷新、切换发布板块清除旧分类、作者调整分类、搜索组合筛选与分页。

人工复查 `categories-1440.png`、`categories-1024.png`、`category-compose-1440.png`、`category-compose-1024.png`、`category-search-1024.png`。未出现横向溢出，分类标识与右侧统计不重叠，窄桌面的发布控件和草稿状态自然换行。未自动归类或重置浏览器既有笔记；测试使用独立浏览器数据。

2026-09-08：新增个人主页来源返回检查，覆盖笔记分页、收藏统计入口、个人回复记录、附件标识和引用块跳转，切换排序并刷新后仍恢复来源路径与滚动位置；删除笔记后也返回来源个人主页。侧栏「个人主页」默认进入笔记标签，其他个人标签保持导航选中。直接访问详情仍显示所属板块返回入口。

单条回复删除验证了所有者权限、取消、持久保存失败、点赞与通知清理、引用删除提示、刷新保持删除、新回复楼层不复用；数据层另外验证匿名参与者称呼不因删除而变化。两种宽度下主帖与回复「删除」按钮均靠右并与点赞同排对齐。复查截图：`profile-entry-1440.png`、`profile-note-return-1440.png`、`reply-delete-1024.png`。附件引用文案为「点击查看」。

本轮新增检查：删除自己的笔记支持取消和 Escape，确认后从首页、搜索、个人笔记／回复／收藏及消息列表消失；数据层拒绝游客、其他作者及重复删除。写入失败时确认窗口保持打开，原笔记与互动仍保留。检查了普通笔记的页面流程和匿名笔记的数据层清理；单元测试确认仍被草稿或头像引用的附件不被清理。

回复排序已验证向上／向下展开、箭头方向、窗口缩放时重新定位、方向键与 Home／End、Enter、Escape、Tab 和点击外部关闭。截图 `reply-sort-up-1024.png` 与 `delete-note-1440.png` 已人工复查。

检查了开场、科大湖、图书馆、教学楼、论坛首页和帖子详情截图。修正了湖边远景建筑与地面的衔接；确认 1024px 论坛无横向溢出，固定导航和入口不遮挡主要内容。完整浏览器测试会生成以下截图到 `test-results/`；单项重跑会清理并仅保留本次产生的截图：

- `campus-1440.png`、`campus-1024.png`
- `lake-1440.png`、`library-1440.png`、`teaching-1440.png`
- `forum-1440.png`、`forum-1024.png`、`topic-1440.png`
- `refined-forum-1440.png`、`refined-forum-1024.png`：新分类、卡片与整体搜索焦点。
- `compose-files-1024.png`、`media-1440.png`、`media-1024.png`：上传编辑器与多媒体详情；两种宽度均无横向溢出。
- `profile-avatar-1440.png`、`reactions-1440.png`：头像更换、粉色爱心与黄色书签。
- `compose-feedback-1440.png`、`compose-feedback-1024.png`：20 字标题与发布控件统一背景反馈。
- `reply-controls-1440.png`、`reply-controls-1024.png`：回复点赞与排序工具栏。
- `aligned-counts-1440.png`、`aligned-counts-1024.png`：固定列宽与万／亿数量缩写。
- `attachment-hints-1440.png`、`attachment-hints-1024.png`：标题末尾附件类型标识与收紧的统计列。
- `source-label-1440.png`：发布页来源名称。
- `reply-subtle-focus.png`：回复上传控件仅有柔和背景反馈。

## 本轮内容入口审查

| 区域 | 审查结果 |
| --- | --- |
| 首页、板块、搜索、个人主题与收藏列表 | 已有整行入口，保留并回归验证；新增附件图标也可进入主题。 |
| 个人中心回复记录、详情引用块 | 从文字链接扩展为整个容器，保持主题和楼层定位。 |
| 右侧今日话题、社区公告 | 扩展为整卡入口；公告列表的次级链接保持独立。 |
| 文件附件 | 从下载文字扩展到整个附件行，缺失文件不生成下载入口。 |
| 消息通知、板块导航、顶部个人入口 | 已有整容器链接，保留。 |
| 视频、点赞、收藏、删除文件 | 保留各自操作，不覆盖为内容导航。 |
| 无目标的社区说明与欢迎横幅 | 保留信息展示用途。 |

参考 [Microsoft Outlook 的列表附件标识](https://support.microsoft.com/en-US/Outlook/mail/open-save-and-edit-attachments-received-in-outlook)：附件以回形针等紧凑标识提示，本项目扩展为三种类型并放在标题末尾。整容器入口参考 [IBM Carbon clickable tile](https://carbondesignsystem.com/components/tile/usage/) 的点击区域原则，同时保留次级链接及媒体控件的独立用途。

## 边界

这是本地交互原型。未验证真实服务端、跨用户同步、学校认证或公开部署，也未将概念插画作为建筑测绘复原。未做 Safari、Firefox 或真实手机专项验收。具体插画参考与未确认的地标细节见 `ART_REFERENCES.md`。

二级分类已实施，完整结构与交互记录在 `SUBCATEGORY_PROPOSAL.md`。旧笔记不自动归类，按未分类兼容。界面名称与反馈术语汇总在 `UI_GLOSSARY.md`；其业务命名是项目约定，不是强制性行业标准。

## 2026-09-08 分类展示精简与分页总数

- 类型检查及生产构建通过（npm run build）。
- 分类、论坛主流程、个人主页返回相关 16 项浏览器检查通过：首轮 15 项通过；新增个人分页测试修正种子数据数量预期后单独复跑通过。
- 检查分类筛选与排序／分页组合、清除后保留板块或回到全部讨论、清除后键盘焦点、刷新及详情返回、草稿预填与修改、搜索范围、匿名展示。
- 检查全部讨论／搜索／个人主页隐藏二级分类，板块列表同时隐藏一级板块，标题预览规则保持。
- 单页与空列表不显示分页；多页保留导航且不再显示底部总数。
- 已人工查看 1440px 和 1024px 板块截图，摘要、列表与统计无重叠，无横向溢出。浏览器测试使用隔离上下文及演示数据，未改动用户浏览器内容。

## 2026-09-08 容器内文字对齐

- 已检查论坛、板块、个人主页笔记／收藏／回复、搜索、详情、发布及消息页，1440px／1024px 无页面横向溢出。
- 单行笔记上下内边距 14px（底部分隔线另占 1px），头像与文字块中点一致。
- 1024px 长标题、长昵称及附件图标隔离数据检查通过；个人主页收藏记录保留摘要且统计不被挤压。
- 19 项相关浏览器回归中首轮 18 项通过，回复按钮对齐发现 1px 差异，修复透明边框占位后该项重跑通过。
- 截图保存在 artifacts/alignment/，测试与截图使用隔离浏览器上下文，未修改用户数据。

## 2026-09-08 搜索及详情精简

- npm run build（含类型检查）通过；12 项数据测试、17 项浏览器回归全部通过。
- 浏览器覆盖唯一搜索分类入口、两级筛选及分页联动、清除后保留关键词、刷新与详情返回、搜索控件聚焦和悬停、发布后分类只读、标题区无重复时间、楼主信息保留发布时间。
- 已查看 1440px／1024px 搜索截图，无横向溢出。
- src 内不存在 setTopicCategory、调整笔记分类、search-board 或重复“输入关键词开始搜索”文案。
- 板块差异化建议及来源记录在 BOARD_LAYOUT_DISCUSSION.md，尚未实现这些建议。

## 2026-09-08 静态编辑背景与列表精简

- 类型检查及生产构建通过；完整 35 项浏览器检查全部通过（约 1.3 分钟）。
- 新增检查覆盖所有列表昵称 6 字缩略、列表不展示时间、个人回复记录同步、详情仅一级静态板块标识、详情完整昵称与发布时间。
- 标题／正文／回复／资料输入 hover 和 focus 不变色，草稿刷新与发布回复正常；原有上传、点赞、删除、匿名、筛选、返回位置等回归通过。
- 1440px／1024px 检查板块、个人主页、发布与回复输入区域，无横向溢出；已人工查看截图。
- 截图在 artifacts/quiet-content/，另有 test-results/quiet-editor-*.png。所有演示检查在隔离浏览器内进行，未修改用户浏览器数据。
